package:
  name: bss
  version: "v2016f.5"

source:
  git_rev: master
  git_url: https://shjoshi@bitbucket.org/bmapdev/bss.git

build:

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  number: 0

requirements:
  build:
    - python
    - setuptools
    - nibabel
    - scipy
    - numpy
    - statsmodels
    - matplotlib

  run:
    - nibabel
    - scipy
    - numpy
    - statsmodels
    - matplotlib
#    - mayavi
#    - jupyter

test:
  # Python imports
  imports:
    #

  #commands:
    # test commands go here

  # requires:
    # additional test requirements go here

about:
  home: http://www.brainsuite.org/bss
  license: GPL
  summary: 'BrainSuite Statistics toolbox'
